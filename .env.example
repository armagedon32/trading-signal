# Copy to .env (never commit .env). All values are optional.
TWELVE_DATA_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Where prediction history is stored (default: .data/predictions.json)
# TRADING_SIGNAL_HISTORY=.data/predictions.json
